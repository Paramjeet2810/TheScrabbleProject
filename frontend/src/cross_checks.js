import {find_word_above, find_word_below} from "./utility";

function cross_checks(board, row, dictionary) {
    let valid_letters = [];
    for (let i = 0; i < board.length; i++) {
        valid_letters.push([]);
    }
    for (let i = 0; i < board[row].length; i++) {
    }
    return valid_letters;
}
